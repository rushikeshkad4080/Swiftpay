import { BUILD_ENV_TYPE, JRPCMiddleware, SafeEventEmitter, WEB3AUTH_NETWORK_TYPE } from "@web3auth/auth";
import { ProjectConfig } from "./interfaces";
/**
 * json-rpc-engine middleware that logs RPC errors and and validates req.method.
 *
 * @param log - The logging API to use.
 * @returns  json-rpc-engine middleware function
 */
export declare function createErrorMiddleware(): JRPCMiddleware<unknown, unknown>;
/**
 * Logs a stream disconnection error. Emits an 'error' if given an
 * EventEmitter that has listeners for the 'error' event.
 *
 * @param log - The logging API to use.
 * @param remoteLabel - The label of the disconnected stream.
 * @param error - The associated error to log.
 * @param emitter - The logging API to use.
 */
export declare function logStreamDisconnectWarning(remoteLabel: string, error: Error, emitter: SafeEventEmitter): void;
export declare const EMITTED_NOTIFICATIONS: string[];
export declare const signerHost: (web3AuthNetwork?: WEB3AUTH_NETWORK_TYPE) => string;
export declare const fetchProjectConfig: ({ clientId, web3AuthNetwork, authBuildEnv, }: {
    clientId: string;
    web3AuthNetwork: WEB3AUTH_NETWORK_TYPE;
    authBuildEnv?: BUILD_ENV_TYPE;
}) => Promise<ProjectConfig>;
