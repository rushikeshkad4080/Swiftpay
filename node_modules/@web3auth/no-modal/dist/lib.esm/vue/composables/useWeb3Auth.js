import { useWeb3AuthInner } from './useWeb3AuthInner.js';

const useWeb3Auth = () => {
  const {
    initError,
    isConnected,
    isInitialized,
    isInitializing,
    isAuthorized,
    provider,
    status,
    web3Auth,
    getPlugin,
    chainId,
    chainNamespace
  } = useWeb3AuthInner();
  return {
    initError,
    isConnected,
    isInitialized,
    isInitializing,
    isAuthorized,
    provider,
    status,
    web3Auth,
    chainId,
    chainNamespace,
    getPlugin
  };
};

export { useWeb3Auth };
